# Smart contract language (Python-like syntax for Ethereum)
vyper==0.4.1

# Ethereum library — connects to nodes, signs txs, calls contracts
web3==6.20.3

# In-memory EVM for testing (no external node needed for tests)
# Note: 0.12.0b1 is the latest version compatible with web3==6.20.3
eth-tester[py-evm]==0.12.0b1

# Testing
pytest==9.1.1

# Web UI
streamlit==1.58.0

# Environment variable loading (.env files)
python-dotenv==1.2.2
